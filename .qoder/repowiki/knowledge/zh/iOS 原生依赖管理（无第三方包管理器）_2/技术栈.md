纯 Apple 原生栈：SwiftUI + SwiftData（iOS 17+）作为 UI 与持久化层，AVFoundation 的 AVSpeechSynthesizer 负责系统 TTS，MPNowPlayingInfoCenter 提供锁屏/控制中心集成，UIKit 仅用于文件选择器桥接；HTTP 请求直接通过 Foundation URLSession 调用阿里云 DashScope API 与 CosyVoice 服务，未引入任何第三方包管理器或 SDK。