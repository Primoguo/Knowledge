- 全局服务类采用 final class + static let shared 单例，并通过私有 init() 禁止外部实例化。
- 对外可变状态一律用 @Published 属性暴露，使 SwiftUI 视图能直接订阅更新。
- 持久化数据通过 UserDefaults.standard 或指定 suiteName 的 NSUserDefaults 读写，键名使用字符串字面量常量（如 defaultsKey）。