- 跨标签共享的可变状态一律在 ContentView 中以 @StateObject 持有，再作为参数注入到子 View，避免在各标签内重复实例化
- 全局 UI 反馈统一走 ErrorHandler.shared.currentAlert，子 View 只负责抛出 alert 而不自行弹窗
- 所有异步数据持久化操作使用 await + MainActor.run { modelContext.insert/save() } 模式，确保在主线程更新 SwiftData