- MVVM 单向依赖：Views 仅持有 ViewModel，ViewModel 调用 Services，Services 读写 Models，禁止反向引用
- 全局主题通过 ThemeManager + UserDefaults 驱动，所有 UI 颜色使用 .accentColor / .primary / .secondary，禁止硬编码 RGB 值
- 配置项统一通过 VoiceConfig 结构体 JSON 编解码后写入 UserDefaults，新增设置需同步更新默认值与迁移逻辑